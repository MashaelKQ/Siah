# Theme

Contains the shared Siah design system.

- `app_colors.dart`: Brand and neutral colors
- `app_text_styles.dart`: Shared typography
- `app_spacing.dart`: Standard spacing values
- `app_theme.dart`: Global Material theme

Screens and widgets should use these shared styles instead of defining random colors, font sizes, or spacing values.